# `@food-ordering/proto`

gRPC proto files and path helpers for Identity, Catalog, Cart, Order.

- Sources: `protos/*.proto`
- Consumers: each Nest microservice + gateway clients

See [docs/API.md](../../docs/API.md) (REST) and service READMEs under `apps/*/`.
