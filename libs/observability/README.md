# `@food-ordering/observability`

Structured JSON logs, Prometheus metrics helpers, OpenTelemetry bootstrap.

Locally set `OTEL_SDK_DISABLED=true`. Stack: [infra/docker/README.md](../../infra/docker/README.md).
