# Helm chart skeletons for local/K8s experimentation ( Documentation §13 / §19.3).
# Cloud Terraform is out of scope for this workspace (local-only decision).
# Full chart values and ArgoCD sync land in Hardening milestone.

charts/
  gateway/
  identity/
  catalog/
  cart/
  order/
  notification/
  realtime/
  web/
  umbrella/
